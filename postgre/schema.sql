CREATE TABLE weather_data (
"Formatted Date" TIMESTAMP,
"Precip Type" TEXT,
"Temperature (C)" REAL,
"Humidity" REAL,
"Wind Speed (km/h)" REAL,
"Pressure (millibars)" REAL,
"Year" INTEGER,
"Month" INTEGER,
"Day" INTEGER,
"avg_Humidity" REAL
);
